apiVersion: v1
kind: Service
metadata:
  name: wolfword
  labels:
    app: wolfword
spec:
  type: ClusterIP
  selector:
    app: wolfword
  ports:
    - name: http
      port: 80
      targetPort: http
